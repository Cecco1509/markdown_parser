#include <gtest/gtest.h>

// CommonMark spec.json conformance suite.
// Placeholder — will load spec.json via nlohmann/json and drive the parser.
TEST(SpecConformance, Placeholder) {
    SUCCEED();
}
